# Task 0001: Global Layout & Design Tokens

## Description
Define the global page scaffold and core design tokens to match the reference screenshots. Establish base layout widths, background treatment, typography, and spacing system used across sections.

## Components / Areas
- Global layout container
- Background pattern (subtle dotted grid)
- Typography scale and font choices
- Spacing scale and radius tokens

## Acceptance Criteria
- Body background matches the light neutral tone seen in screenshots.
- A subtle dotted grid/pattern appears behind the hero area on mobile/tablet and is absent on desktop/desktop-large.
- Define explicit container max-widths and gutters per breakpoint (mobile ~360, tablet ~810, desktop ~1200+); apply consistently across sections.
- Typography scale supports hero headline, section headings, body copy, and small labels; font family is explicitly chosen and used globally.
- Spacing and border-radius tokens are defined and used consistently.
- No layout shift occurs when fonts load (header and hero height remain stable).

## Estimate
3

## Status
Verified

## QA Notes (2026-02-20)
- Re-verified with Playwright at 360px, 810px, 1200px, and 1440px; background appears near-white and the dotted grid only renders on mobile/tablet.
- Container widths/gutters and typography/radius tokens confirmed in `src/app/globals.css`.
- Playwright console reported an extension error (`chrome-extension://...`), not attributable to the app.
- Previously flagged: desktop/desktop-large background too beige vs reference (resolved).
- Previously flagged: mobile/tablet dotted grid contrast too strong on warmer background (resolved).

## Dev Notes (2026-02-20)
- Updated global background to near-white and reduced dotted grid contrast/size.
- Playwright MCP could not open PDFs directly; converted reference PDFs to PNGs via `sips` for side-by-side verification.

## Dev Notes (2026-02-19)
- Adjusted `--color-bg` to near-white and softened the dotted grid (smaller dots, lower opacity).
- Re-verified at 360px, 810px, 1200px, and 1440px via Playwright MCP.
